import { YesNoValidatorDirective } from './yes-no-validator.directive';

describe('YesNoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new YesNoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
